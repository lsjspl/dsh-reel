# dsh-reel

在 dsh 的端口上开一个图片 / 视频浏览页：同端口、同源，无新进程、无构建，对用户文件只读。一个列表页（目录树 + 网格/列表 + 图片查看器）加一个播放器——**手机上点开视频就是整页，上下滑换下一条**，顺序或随机。视频走 HTTP 范围响应，拖动进度条即时起播。列表只列当前这一层；子目录从左侧目录树、`▾` 下拉或网格卡片进，树顶的「库」则一次聚合全部配置目录。

```
http://127.0.0.1:3080/reel
```

## 安装

从 npm：

```powershell
dsh plugin --profile web add dsh-reel
dsh web
```

不是 `npm install`：插件要注册进 profile 的 bundle 列表才会被 dsh 加载，这条命令（转给 pnpm 装包 + 注册）一并完成。

从 Git（不等发版）：

```powershell
dsh plugin --profile web add "github:lsjspl/dsh-reel"
dsh plugin --profile web add "git+https://github.com/lsjspl/dsh-reel.git#main"   # # 后跟分支/tag
```

本地开发（软链，改源码立即生效）：

```powershell
dsh plugin --profile web add "link:C:\path\to\dsh-reel"
```

卸载：

```powershell
dsh plugin --profile web remove dsh-reel
```

## 指定目录

两处入口，数据是同一份（`settings.yaml` 的 `reel` 命名空间），改动热生效：

- **dsh 设置 → 插件 → 插件配置 → Reel 媒体浏览**：卡片里增删目录、配置缓存目录、配置**全屏壁纸**（填媒体 key 或目录里的绝对路径，留空不设；这是所有设备的默认，浏览页上还能用 🖼 临时换成本机自己挑的）；顶部有「打开 Reel」按钮和项目 GitHub 链接。
- **profile 的 `cordis.patch.yml`**（组合行配置）：

```yaml
- id: reel
  config:
    roots:
      - D:\Photos
      - E:\电影
    # cacheDir: 'D:\dsh-cache'   # 视频封面/悬停动画缓存，缺省在系统临时目录
```

也可以用 `REEL_ROOTS` 环境变量（`;` 分隔，仅在没有其他配置时生效）。都没有时回退 `$DSH_HOME/media`。

浏览页自身没有目录配置入口，接口里也没有媒体目录之外的目录列举能力；缓存目录别放在媒体根里面——缓存文件会出现在浏览页，插件会记一条警告。

## 功能

**列表模式**

- 左侧目录树：树顶的「库」聚合**所有配置目录**（选中即递归列出全部内容，筛选/搜索同样覆盖全库），下面才是各配置目录的根；子目录按需加载
- 内容三选一（快捷键 `G` 轮转）：**当前**（只列这一层，平铺）· **全部**（递归整个子树，平铺成一屏）· **分组**（递归整个子树，按目录分成可折叠的嵌套分组，组头显示子树媒体数，另有「展开全部 / 折叠全部」）
- 图片 / 视频过滤，视图（网格 / 列表），**网格大小三档**（小 / 中 / 大）：
  - 像素不是写死的，按设备基准算（桌面 200px、窄屏 132px、小屏 112px）：桌面「中」是 5 列、「小」7 列、「大」3 列
  - **手机上「大」是一屏一张**（`--tile-wide` 在窄屏带一条相对屏宽的下限），「中」3 列、「小」4 列
  - 窄瓦片（<118px）下卡片自己收：格式标签让位、尺寸行单行省略、播放圈缩到 34px
  - 选哪档记在本地（`mv.tileSize`），列表视图里那组按钮自动收起
- 路径行和右边的按钮同一行：点名字直接进那一层，`▾` 挑这一层的子文件夹，`‹` 回上一级（`Backspace`）；路径深了自动把中间层级收成一枚 `…`，点开才摊平
- 视频封面取中间帧，悬停播放 4 秒静音小动画（需 ffmpeg，没有则退化）
- 加载全程静默，续页滚到底自动拉
- 图片查看器：先开成页面上的窗口，点右上角「页面全屏」才铺满整页；滚轮缩放、拖动平移、`←/→` 翻页
- 快捷键：`G` 内容模式 · `V` 网格/列表 · `S` 排序 · `R` 重载 · `B` 收起目录树 · `/` 筛选
- 顶栏「**▶ 看视频**」：打乱当前列表，用播放器**铺满这一页**从随机一条开始连着放（顺序切到「随机」，⇄ 那枚会跟着点亮）

**全屏壁纸**

- 图片查看器 / 播放器上那枚 🖼 把当前这条设成整页背景，**图片和视频都行**（视频静音循环，切到后台自动暂停；系统开了「减少动态效果」就只留第一帧）
- 壁纸层压在界面之下、`pointer-events: none` 不吃点击；界面各层转成半透明 + 模糊，壁纸从底下透出来（浅色主题下是白色玻璃）
- 选哪条记在本地（`mv.wallpaper`，存的是**能直接用的 URL** 而不是 key——刷新后列表还没回来，壁纸得先画出来），刷新 / 重开还在
- 同一条上再点一次那枚 🖼 就取消（手机端播放器那枚跟着其他不常用的按钮一起收起，图片查看器那枚仍然在）

**播放器（手机上就是「刷」的界面）**

- **手机 / 触屏上点开就是整页**：不用再点一次「页面全屏」；桌面仍是先给窗口，想铺满再点
- **上下滑 = 上一条 / 下一条**（触摸手势；桌面用 `↑` `↓` / `⏮` `⏭`），播完自动接下一条
- **顺序 / 随机**：控件条上那枚 🔀，随机是打开时打乱一次、当前这条永远排最前，之后
  上下滑与连播都按这份次序走；选择记在本地
- 连播列表是**整份当前列表**（当前目录 / 全部 / 库，跟着筛选和排序走），不是屏幕上那几张卡
- 关闭与全屏相关的东西只在视频上方那条浮动条上，下面的控件条只管播放
- 即时拖动起播，断点续传
- 0.25×–3× 倍速、长按 2× 快进、续播、连播
- 自动加载同名字幕（SRT / ASS），截图、下载、统计面板
- **老格式（AVI / WMV / FLV / RMVB 等浏览器自己解不开的容器）**：服务端用同一个 ffmpeg **边转边播**——从你要看的位置开始转，fragmented MP4 边转边发，**不落磁盘、不做缓存**；一秒左右出画，拖动进度条则从目标时间重开一条流（再等一两秒）。CPU 只花在真正看过的部分上（需 ffmpeg）
- `空格`/`K` 播放暂停 · `←/→` 5 秒 · `J`/`L` 10 秒 · `C` 字幕 · `F` 全屏

**手机端**

- 点开视频就是整页播放，上下滑换条（见上面的播放器一节）
- 图片：双指捏合缩放、横滑翻图，safe-area 适配
- 局域网访问：`dsh web --host 0.0.0.0`，手机先打开启动输出的带 token 地址完成信任，再进 `/reel`

## 安全

- 路径包含性经 `realpath` 验证，`..` 与外部软链出不去
- 页面只见配置过的目录：没有任何媒体根之外的目录列举或搜索接口
- 对媒体目录只读；封面与悬停预览缓存在系统临时目录或配置的缓存目录，边转边播的转码流不落盘
- 默认复用 dsh 的浏览器信任闸门，LAN 访问需先过信任

## 侵入面

组合树一行、只占 `/reel` 与 `/reel/*` 路由、零依赖、无构建。详见 `cordis.patch.yml`。

## 测试

```powershell
node tests/run.mjs       # 接口独立用例，不需要 dsh
node tests/library.mjs   # 「库」节点 + 老格式转码播放：假 dsh + 真实 Chrome

# 想连「AVI → MP4 转码」的正向链路一起验证：指向真实的 ffmpeg 再跑上面两条
$env:REEL_FFMPEG = 'C:\path\to\ffmpeg.exe'
```

其余（live / mobile / layout / verify-ui / chunks）针对运行中的实例，见 `tests/` 目录。其中 `chunks` 专门盯平铺网格的分块窗口化：滚动全程视口里不许出现未挂载块留下的空白。

## 项目

源码与问题反馈：<https://github.com/lsjspl/dsh-reel>

## License

MIT
