# dsh-reel

在 dsh 的端口上开一个图片 / 视频浏览页。两种模式：**列表模式**（目录树 + 网格/列表 + 图片查看器）和**刷视频模式**（一屏一条的纵向流，像抖音那样上下滑）。视频走完整的 HTTP 范围响应，所以拖动进度条是**即时起播**，不是重新缓冲。

- 目录在设置里指定，想加几个加几个，任意路径。
- 页面和 dsh 同一个端口、同一个源，不需要再起进程、不需要新端口、不需要构建。
- 对用户文件**只读**：唯一的写操作是写 dsh 自己的 `settings.yaml`（保存你填的目录）。

```
http://127.0.0.1:3080/reel
```

---

## 安装

在插件目录下执行（`dsh plugin` 会把参数原样转给 profile 里的 pnpm）：

```powershell
# 1) 装进 dsh 的 web profile，并在 profile 的 bundle 列表里激活它
dsh plugin --profile web add "link:C:\Users\lsj\Desktop\IDEAProject\dsh-reel"

# 2) 重启 dsh web
dsh web
```

`link:` 会建一个软链，改完插件源码立刻生效（推荐，尤其是自己改插件的时候）。用 `file:` 的话 pnpm 会**复制**一份到 profile 里，改源码后要 `dsh plugin --profile web install` 才会同步。

装完 `~/.dsh/profiles/web/package.json` 会长这样，这就是全部的侵入面：

```json
{
  "dependencies": { "dsh-reel": "link:..." },
  "dsh": { "profile": { "bundles": ["@deepseek-ai/dsh-base", "@deepseek-ai/dsh-web-app", "dsh-reel"] } }
}
```

不想用了：

```powershell
dsh plugin --profile web remove dsh-reel   # 依赖和 bundle 层一起摘掉
```

更干净的做法是**不动现有的 web profile**，另开一个自带 web 界面的 profile：

```powershell
# 先按 web 模板建出 profile（不启动，只建）
dsh --profile media --from-default-profile web --dump-config
# 再把它自己装进去
dsh plugin --profile media add "link:C:\Users\lsj\Desktop\IDEAProject\dsh-reel"
# 用 --profile 启动自定义 profile；`dsh web` 恒等于 --profile web
dsh --profile media --no-open
```

---

## 指定目录

三处入口，解析优先级从高到低：

| 方式 | 说明 |
|---|---|
| **打开页面 → 右上角设置** | 内置目录浏览器，点着选，可加多个、可移除。写进 `settings.yaml` 的 `reel.roots`。 |
| **dsh 设置页 → 插件 → 插件配置** | 同一份数据（`reel` 命名空间），两处永远一致。 |
| **`REEL_ROOTS` 环境变量** | 没有配置任何目录时生效，用 `;` 分隔。 |

都没有时回退到 `$DSH_HOME/media`（不存在就不显示任何目录，页面会提示你去设置）。

也可以直接写进插件的组合行，在 `~/.dsh/profiles/<profile>/cordis.patch.yml` 里：

```yaml
- id: reel
  config:
    roots:
      - D:\Photos
      - E:\电影
    requireTrustedRequest: true
```

设置里的修改是**热生效**的，不用重启 dsh。

---

## 手机端

支持，而且是按手机的操作方式做的，不是把桌面版缩小：

| 场景 | 手机上的操作 |
|---|---|
| 列表模式 | 单列 / 双列网格自适应，侧边目录树自动收起（改路径靠面包屑和文件夹芯片）。 |
| 图片查看器 | **双指捏合**缩放（以双指中心为不动点）、单指拖动平移、**单指横滑**翻上一张/下一张。 |
| 刷视频 | 上下滑吸附切换，**左右滑**也能切上一条/下一条；点画面暂停/播放；右侧竖排操作按钮 42px 触摸目标。 |
| 播放器 | 进度条加高到 7px 且拖动区更宽，专门为手指调过；长按快进只对鼠标启用（手指长按在手机上会弹系统菜单）。 |
| 布局 | 顶栏收紧到 52px，播放器不常用按钮隐藏，底部留 `env(safe-area-inset-bottom)` 避开 Home 指示条，窄屏没有横向溢出。 |
| 流量 | 触摸端刷视频只取 250 条（桌面 600），滚动时只挂当前条前后各一条 `<video>`，图片靠 `loading=lazy` 只在进入视口时取。 |

### 手机怎么连上

dsh 默认只监听回环地址，手机得先能访问到它：

```powershell
dsh web --host 0.0.0.0          # 允许局域网访问
```

启动那行会多打一个局域网地址（带一次性 token），**先用手机浏览器打开那个地址**完成一次 token 交换、拿到签名 cookie，然后再进 `/reel`。插件默认复用 dsh 自己的浏览器信任闸门（和 `/api` 同一套规则），所以没有 cookie 的请求会被挡掉——这是故意的，避免 LAN 绑定下媒体库变成没有门的洞。

> 你的 profile 里如果已经有 `webserver` 那一行的补丁把 `host` 写成 `0.0.0.0`（这个仓库的开发配置就是这样），那就已经开着，注意它是**故意**的局域网暴露。

### 触摸端的验证方式

不是靠肉眼看代码，而是用 headless Chrome 开一个 iPhone 视口（390×844、触摸模拟、移动 UA）真的去点、去滑、去捏：

```powershell
node tests/mobile.mjs http://127.0.0.1:3090
```

25 项，覆盖布局不溢出、触摸目标尺寸、点卡片开查看器、横滑翻页、双指捏合放大/收拢、刷视频上滑切换、以及**控制台必须没有异常**。捏合用的是 CDP 的真实多点触摸序列，而不是页面里合成的 `PointerEvent`——只有走浏览器自己的命中测试，`touch-action` 与 `pointercancel` 那条路径才会被真正执行到。

---

## 两种模式

### 列表模式

- 左侧目录树，**每个配置的目录都是一个可展开的根**（只画当前那个会让别的根在视觉上消失）；子目录按需加载，展开时才读那一层。
- 右侧：上面是工具条（面包屑 + 三个开关），下面先列**子文件夹（一行一个，紧凑可扫）**，再列媒体卡片。文件夹不再画成大卡片——目录名常常上百字符，卡片化只会又丑又占地方。
- **三个开关**：
  - **范围**：`本目录` / `全部（含子目录）`。后者递归列出所有层级的文件、不显示文件夹，并在每条下面标出它来自哪一层（点标签直接跳过去）。
  - **内容**：`全部` / `只看视频`。
  - **分组**：`平铺` / `按文件夹分组`——每个文件夹包成**一张卡片**，卡片**内部照旧平铺**它的内容；卡片内的列宽按该文件夹的文件数自适应（列数 ≈ √数量，下限 84px），所以文件少的缩略图大、文件多的自动变小，不会出现「一张卡里两个巨图」或者「一张卡里几十粒米」。两个范围都支持：`本目录` 时一个子文件夹一张卡（滚动到才加载，同时最多读 4 层），`全部` 时按每条文件所在的目录现成归堆、立刻铺开、不再发请求。
- **加载全程静默**：没有全局加载遮罩、没有转圈、没有骨架闪烁、没有「载入更多」按钮。续页滚到底自动拉，封面取不到就安静退成图标。（唯一保留的转圈在播放器里：那是用户主动点开视频后的必要反馈。）
- 视频卡片用**封面帧**（服务端取视频**中间**那一帧），鼠标停上去会放一小段**中间几秒的静音小动画**；取不到封面就退成图标占位。
- **目录栏宽度可拖**：拖分隔条即可（200–560px），双击回默认 320px，宽度记在本地。
- **目录栏可收起**：收起后左侧留一条窄栏，点它或按 `B` 拿回来。这个状态**不跨刷新记住**——否则上次收起、这次打开什么都没有，看上去就是「根本没有侧边栏」。
- **树的点击语义：「展开即浏览」**。点一个目录（根或子目录）= 展开它**并把右侧列表切到这个目录**；再点同一行收起，**右侧列表不动**。想只折展而不动列表，点行首的三角；想直接进目录，用行尾的 `›` 或双击。当前目录所在的那条路径会自动展开（即使之前折过），否则你点了进目录、树里却看不到自己在哪。
- `V` 切网格/列表，`S` 切排序（最新 / 最旧 / 名称 / 体积 / 类型），`R` 重载，`B` 收起/展开目录树，`/` 聚焦筛选。
- 点图片 → 全屏查看器：滚轮缩放、拖动平移、`←/→` 翻页、`D` 下载。
- 点视频 → 全屏播放器；播放器里的「下一个」就是同一目录里的下一个。
- 「▶ 全部连播」把当前目录递归扫成一整条播放列表。

### 刷视频模式

- 一屏一条，`scroll-snap` 纵向吸附，鼠标滚轮 / 触屏滑动 / `↑` `↓` 切换。
- 视频静音自动播放、循环、播到底自动切下一条；图片按 6 秒节奏自动前进，带进度条。
- 右侧操作栏：全屏播放、下载、看文件信息。
- 左上角「范围」按钮切换只刷**当前目录**还是刷**整个根目录**（递归）。
- 只挂载当前条前后各一条的 `<video>`，滚过几百条也不会把解码器堆爆。
- `M` 静音开关，`空格` 暂停，`Esc` 回列表。

---

## 播放器

| 能力 | 说明 |
|---|---|
| 即时拖动 | 宿主对每个请求回 `206` + 精确 `Content-Range`；带 `If-Range` / `ETag` / `Last-Modified`，断点续传不会把两个版本的文件拼在一起。 |
| 键盘 | `空格`/`K` 播放暂停 · `←`/`→` 5 秒 · `J`/`L` 10 秒 · `↑`/`↓` 音量 · `M` 静音 · `[`/`]` 倍速 · `0` 复位 1× · `C` 字幕 · `I` 统计 · `U` 单曲循环 · `P` 截图 · `Q` 画中画 · `F` 全屏 · `D` 下载 · `Home`/`End` 首尾 · `Esc` 关闭。 |
| 倍速 | 0.25× – 3×，记住上次选择。 |
| 长按快进 | 按住画面 2 倍速，松开恢复。 |
| 续播 | 每个文件的位置存在浏览器本地，下次打开自动跳回去并提示。 |
| 字幕 | 自动找同目录的同名字幕（`movie.srt` / `movie.zh-CN.srt` / `movie.ass`…），SRT 和 ASS 都由宿主转成 WebVTT。 |
| 截图 | 截当前帧存 PNG，文件名带时间戳。 |
| 统计面板 | 分辨率、时长、缓冲区间、已解码帧、丢帧率、这次请求的实际传输字节与耗时。 |
| 连播 | 播完自动下一条，整个目录按当前排序当播放列表。 |

浏览器能不能解码由浏览器决定：MP4(H.264)/WebM 基本都能播，MKV/HEVC/AV1 取决于浏览器。播不了时会明确提示，而不是丢一个黑屏给你。

---

## 视频封面（缩略图）

视频卡片上的封面是**视频中间那一帧**，不是第一秒：开场常常是黑屏、台标或标题卡，取第一秒会让整个网格长得一模一样。

具体做法：先用 ffprobe 问出时长，在**输入侧** `-ss 时长/2` 快速定位到中间（不解码前面的内容），再由 ffmpeg 的 `thumbnail` 过滤器在附近挑出最有代表性的一帧。每个文件只解码一帧，代价与视频长度无关（实测 2 小时 / 5GB 的文件 261ms）。

- 封面按 `路径 + mtime + size` 缓存在系统临时目录，第二次看是一次 `readFile`（实测 261ms → 6ms），**用户目录永远不被写入**。
- 同时最多 3 个 ffmpeg 在跑，超出的排队——一次给几百张卡片生成封面不会把 CPU 打满。
- 失败的渲染记一笔（6 小时内不重试），避免一个解不开的文件在每次滚动时都重来一遍。
- 客户端只在卡片进入视野时才请求封面，没出图时显示会呼吸的骨架，取不到才退成图标。

### 悬停播放小动画

鼠标在视频卡片上停 350ms，就会在上面放一小段**从中间截取的 4 秒静音动画**（VP9/WebM、320px、12fps，实测约 25–170KB、生成 0.6–1.3s，第二次走缓存 30ms 级）。几秒钟的动态比任何一帧静图都更能说明这是什么视频，而因为是从中间截的，永远不会是开头的标题卡。

- 只对**鼠标**启用（`(hover: hover)`）；触摸端没有悬停，不做无谓的流量。
- 指针离开、卡片被重画、滚出视野都会立刻停掉并拆掉 `<video>`，不会在后台继续解码。
- 和封面共用同一个并发队列（上限 3），所以一屏疯狂划过去也不会起一堆 ffmpeg。
- 拿不到动画（没有 ffmpeg、容器解不开）就安静退回静态封面。

### ffmpeg / ffprobe 从哪来

**不是只查 PATH。** Windows 上 `winget install ffmpeg` 会把 shim 放进 `%LOCALAPPDATA%\Microsoft\WinGet\Links`、真实二进制放进 `WinGet\Packages\...`，而那个 Links 目录经常不在进程的 PATH 里——本插件就是在这台机器上踩到这个才改成多路探测的。查找顺序：

1. 配置里的 `ffmpegPath`
2. PATH
3. `%LOCALAPPDATA%\Microsoft\WinGet\Links\ffmpeg.exe`
4. `%LOCALAPPDATA%\Microsoft\WinGet\Packages\*ffmpeg*\*\bin\ffmpeg.exe`（扫一层，版本号会变）
5. `~/scoop/shims/ffmpeg.exe`、Chocolatey、`%LOCALAPPDATA%\Programs\ffmpeg`、`C:\ffmpeg\bin`
6. 非 Windows：`/usr/bin`、`/usr/local/bin`、`/opt/homebrew/bin`

结果按进程缓存（探测一次要遍历 PATH，实测约 20ms；之前每个文件都探一次，143 个文件的扫描因此慢了 3 秒）。

也可以显式指定：

```yaml
- id: reel
  config:
    ffmpegPath: 'C:\tools\ffmpeg.exe'
```

**没有 ffmpeg 也能用**：封面退回浏览器的 `preload="metadata"` 预读（能打开首帧的容器都行），时长退回浏览器自己报，功能不缺，只是 MKV 之类打不开的容器拿不到封面和时长。

`ffprobe`（和 ffmpeg 同目录）存在时，卡片时长与分辨率由服务端批量提供——一次最多 200 个 key，服务端限 4 并发并按 `路径+mtime` 缓存。这比让浏览器一个个去读头强，尤其是它根本打不开的容器。

---

## 安全边界

- **路径包含性是证明出来的**，不是字符串前缀比对：请求路径和根目录都过 `realpath`，所以 `..` 和指向外部的软链都出不去。
- **只读**：对配置目录只做 `readdir` / `stat` / `open` / 读文件，缩略图缓存在系统临时目录。
- **默认复用 dsh 自己的浏览器信任闸门**：非回环地址的请求交给 `connection.requestRejection` 判定，和 `/api` 同一套规则——LAN 绑定下它不会变成一个没有门的洞。组合里没有这个服务时退化为「必须同源」，`requireTrustedRequest: false` 可以显式关掉。

---

## 对 dsh 的侵入面

这是刻意压到最小的：

| 项 | 内容 |
|---|---|
| 组合树 | 一行：`reel`。删掉即下线，不用卸载包。 |
| 路由 | `/reel` 与 `/reel/*`，是 dsh 从没占用的路径空间，不碰任何已有路由。 |
| 依赖 | **零**。宿主半边只 import Node 内建模块；浏览器半边是原样静态文件。 |
| 构建 | **无**。没有 tsdown、没有 client bundle、没有 `pnpm run build`。 |
| dsh GUI | 不改现有设置页、不注册客户端插件。配置界面在自己的页面里。 |
| 设置 | 注册一个自己的命名空间 `reel`，只在组合里存在 settings 服务时挂载；没有就退回组合行配置继续工作。 |

---

## 目录结构

```
dsh-reel/
├── cordis.patch.yml       # 唯一的组合侵入点：插入 reel 行
├── package.json           # dsh.bundle.patch 指向上面这个文件
├── lib/
│   ├── reel.js    # 宿主半边：路由、范围流、目录扫描、设置命名空间
│   ├── media.js           # 类型判定、Range 解析、包含性检查、字幕转 VTT
│   └── assets/            # 浏览器半边，原样发送，无构建
│       ├── index.html
│       ├── app.css        # 含触摸端媒体查询与 touch-action 策略
│       ├── app.js         # 页面逻辑：两种模式、目录树、手势、设置抽屉
│       └── player.js      # 播放器：控件、快捷键、字幕、续播、统计
└── tests/
    ├── run.mjs            # 115 项：假上下文 + 真 HTTP，覆盖范围语义与安全
    ├── live.mjs           # 57 项：打真正由 dsh 起起来的服务
    ├── mobile.mjs         # 29 项：headless Chrome 手机视口，真触摸手势
    ├── layout.mjs         # 21 项：桌面布局几何（目录栏宽度、拖动、列表列宽）
    └── verify-ui.mjs      # 69 项：多根、展开即浏览、分组、封面、悬停动画、折叠恢复、静默加载
```

## 测试

```powershell
node tests/run.mjs                                        # 独立用例，不需要 dsh
node tests/live.mjs http://127.0.0.1:3090                 # 真机（先按上面方式起一个实例）
node tests/mobile.mjs http://127.0.0.1:3090               # 手机视口 + 触摸手势（需要 Chrome）
node tests/layout.mjs http://127.0.0.1:3090               # 桌面布局几何（需要 Chrome）
node tests/verify-ui.mjs http://127.0.0.1:3090            # 界面结构 + 封面 + 分组 + 悬停动画（需要 Chrome）
```

`tests/run.mjs` 里最要紧的一组断言是范围流：整文件、开区间、后缀区间、越界 416、多段降级、`If-Range` 失配回整文件，以及**分段取完之后拼起来必须和源文件逐字节相同**——视频能不能拖，全看这一条。另外还有设置分层的回归用例（命名空间 schema 的空默认值不能盖掉手写的 `roots`），以及空 key 等于「第一个根目录」的回归用例。

`tests/mobile.mjs` 和 `tests/layout.mjs` 会把 `CHROME_PATH` 或默认路径的 Chrome 用 `--headless=new` 起起来，用 CDP 做设备模拟，所以触摸、捏合、`pointercancel`、真实鼠标拖动都是真的。两者都会先问服务端「哪个目录有媒体」，再按实际找到的内容决定测什么——真实媒体库里根目录可能只有分类目录、也可能一张图片都没有，写死 fixture 路径只会制造假失败。

### 改 profile 之前先演练一次

动 `dsh.profile.bundles` 会直接影响下一次启动能不能起来，所以有一个不改任何东西的启动演练：把真实 profile 的整棵树装一遍、确认 reel 的行激活、10 条媒体路由都注册上、再真打一次接口，然后立刻拆掉。

```powershell
cd $env:USERPROFILE\.dsh\profiles\web
Copy-Item <插件目录>\tools\boot-check.mjs .
node .boot-check.mjs web      # 加 --no-listen 就完全不监听端口
Remove-Item .boot-check.mjs
```

必须放在 profile 目录里跑，否则解析不到 `@deepseek-ai/*`。不加 `--no-listen` 时它会临时监听 3089，跑完自动释放。典型输出：

```
reel 行  : 存在，fiber 状态 2（2=active）
webServer        : 监听端口 3089
路由             : 全部 10 条已注册 ✓
session 响应     : HTTP 200, roots=0, 可写=true, ffmpeg=null
```

## 排查

组合树里有行但接口 404，几乎总是这两个原因之一：

1. **行里少了 `inject: [webServer]`**。HTTP handler 跑在插件体之外，没有这行注入，第一次读 `ctx.webServer` 就是 `cannot get property "webServer" without inject`。`cordis.patch.yml` 里已经带上了，自己重写该行时别丢。
2. **`dsh web` 跑的不是你装插件的那个 profile**。`dsh web` 恒等于 `--profile web`；装到了别的 profile 就用 `dsh --profile <名字>` 启动。

想看插件有没有真的起来、路由有没有注册上：

```powershell
$env:REEL_DEBUG = "1"
dsh web
# 启动过程写进 %TEMP%\dsh-reel-trace.log
```

页面上某处「功能在跑但看不见」时（这个项目里真发生过：刷视频渲染完了 5 条，却始终显示在隐藏容器里），先看有没有 `页面元素缺失：…` 的提示——`app.js` 启动时会核对自己引用的每个 id，缺一个就明说，而不是让某个模式静默失效。

---

MIT
